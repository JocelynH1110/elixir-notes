<!--用 @changeset 建立一個表， 當在表格上按下完成鈕，送到和 topic 有關的 :create router-->
<%= form_for @changeset, topic_path(@conn, :create), fn f -> %>
  <div class="form-group">
    <%= text_input f, :tittle, placeholder: "Title", class: "form-control" %>
  </div>

  <%= submit "Save Topic", class: "btn btn-primary" %>
<% end %>
